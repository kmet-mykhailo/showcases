{
  "name": "rxjs-demo",
  "version": "1.0.0",
  "description": "Demo of basic RxJs functions",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "cleanup": "ts-cleanup -s src -d dist -v",
    "build": "npm run cleanup && tsc --build",
    "start": "node dist/index.js",
    "watch": "tsc --watch",
    "test": "jest",
    "dev": "ts-node src/index.ts"
  },
  "author": "Mykhailo Kmet",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^24.9.1",
    "rxjs": "^7.8.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "rimraf": "^6.0.1",
    "ts-cleanup": "^1.3.0",
    "ts-jest": "^29.4.5"
  }
}
